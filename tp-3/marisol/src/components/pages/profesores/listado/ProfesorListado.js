import React from 'react';

// Since this component is simple and static, there's no parent container for it.
const ProfesorListado = () => {
  return (
    <div>
      <h2>Profesor List</h2>
    </div>
  );
};

export default ProfesorListado;